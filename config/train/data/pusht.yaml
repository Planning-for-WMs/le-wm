dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 5
  name: pusht_train_${img_size}.lmdb
  cache_dir: ${hydra:runtime.cwd}/dataset
  keys_to_load:
    - pixels
    - action
    - observation
  keys_to_cache:
    - action
    - observation
